inventory/
├── cmd/
│   └── api/          # Точка входа в приложение
│       └── main.go   # Здесь инициализируем и запускаем всё
├── internal/         # Приватный код вашего приложения (не для импорта извне)
│   ├── model/       # Ядро! Сущности и бизнес-правила
│   ├── service/      # Бизнес-логика, "сценарии использования"
│   ├── repository/   # Интерфейсы для работы с данными (как "контракты")
│   ├── handler/      # Обработчики HTTP-запросов (контроллеры)
│   └── pkg/          # Внутренние вспомогательные пакеты (например, валидатор)
├── pkg/              # Публичные вспомогательные пакеты (пока можно оставить пустым)
├── migrations/       # SQL-скрипты для обновления схемы БД
├── tests/            # Интеграционные и e2e тесты
├── go.mod           # Файл модуля (уже есть)
└── go.sum           # Файл контрольных сумм зависимостей

Get-ChildItem -Path $folder -File -Recurse | ForEach-Object {
>>     Write-Host "📄 Файл: $($_.Name)" -ForegroundColor Cyan
>>     Write-Host "-------- Содержимое --------"
>>     Get-Content $_.FullName
>>     Write-Host "----------------------------`n"
>> }